from utils import find_max

numbers = [10, 3, 6, 50, 2, 100]
maximum =find_max(numbers)
print(maximum)


# find_max() # list returns large number in the list
# extract the function from here and add in the utils model
